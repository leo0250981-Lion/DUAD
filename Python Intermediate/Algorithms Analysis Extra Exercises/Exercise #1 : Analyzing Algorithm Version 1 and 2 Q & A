# Version 1 of Algorithm 
# def manual_add(n):
#    result = 0
#    for i in range(1, n + 1):
#        result += i
#    return result

# Time Complexity

# The loop runs n times.

# So the work grows proportionally with n.

# O(n)

# Space Complexity

# Only a few variables → no extra structures.

# O(1)
# Version 2 of Algorithm
# def add_formula(n):
#    return n * (n + 1) // 2
# Time Complexity

# No loops.
# Just arithmetic operations.

# No matter if n = 10 or n = 1 billion → same effort.

# O(1) (constant time)

# Space Complexity

# Also constant.

# O(1)

# Because Explanation:

# manual_add:  would need 1 billion iterations 

# add_formula:  does it instantly.

# Summary 
# manual_add  → O(n)
# add_formula → O(1)

# For very large values of n, the formula version is preferable
# because it computes the result in constant time and avoids
# iterating through all numbers.
